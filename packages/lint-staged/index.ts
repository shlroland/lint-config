const config = {
  "*.{ts,tsx}": ["eslint --fix"],
  "*.{js,jsx}": ["eslint --fix"],
  "*.{css,scss,sass,less}": ["eslint --fix"],
  "*.{md,mdx}": ["eslint --fix"],
  "*.{json,yaml,yml}": ["eslint --fix"],
};

export default config;
